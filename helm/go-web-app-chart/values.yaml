replicaCount: 1

image:
  repository: shantanusarode/go-web-app
  pullPolicy: IfNotPresent
  tag: "16033870239"

ingress:
  enabled: false
  className: ""
  annotations: {}
    # kubernetes.io/ingress.class: nginx
    # kubernetes.io/tls-acme: "true"
  hosts:
    - host: chart-example.local
      paths:
        - path: /
          pathType: ImplementationSpecific